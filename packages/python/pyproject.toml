[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentctl-client"
version = "0.1.0"
description = "High-level Python client and guard helpers for agentctl"
requires-python = ">=3.10"
license = { text = "MIT" }
